#!/usr/bin/env bash
set -euo pipefail

echo "[mail-service hook] After bash commands, capture whether typecheck/build ran and whether docs changed. If you changed responsibilities, added project skills, or introduced tool-backed workflows, update the matching AGENTS/skill/hook guidance and keep package.json scripts and packages in sync."
