# AGENTS

This file applies to `src/lib/server/providers/imap/`.

## Rules

- Keep the canonical IMAP implementation in `src/services/server/providers/imap/**`.
- Leave only compatibility wrappers here unless the task is explicitly about old import support.
