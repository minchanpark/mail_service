# AGENTS

This file applies to `src/other/theme/`.

## Rules

- Put shared theme tokens or view-agnostic presentation constants here when they are reused.
- Keep global CSS entrypoints in `src/app/**` when the framework requires them there.
